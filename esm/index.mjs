// .js -> Utliza por  defecto CommonJS (cjs)
// .mjs -> Utiliza por defecto ES Modules (esm)
// .cjs -> Utiliza por defecto CommonJS (cjs)

//Podemos utiliza el import
import { suma } from './suma.mjs'

console.log(suma(1,10));
